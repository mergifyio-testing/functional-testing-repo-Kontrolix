pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - label=queue
  name: default merge
queue_rules:
- name: default
  routing_conditions:
  - base=20230616041907/test_merging_a_stack_1/main
