jobs:
  unit-tests:
    runs-on: ubuntu-20.04
    steps:
    - name: 'run version: ${{ matrix.version }} os:${{ matrix.os }}'
      run: 'echo version: ${{ matrix.version }} os:${{ matrix.os }}'
    strategy:
      matrix:
        os:
        - wind, ows
        - mac
        version:
        - 2.8
        - 3.5
    timeout-minutes: 5
name: Continuous Integration
'on':
  pull_request:
    branches: 20230927092705/test_log_embedding_matrix/main
